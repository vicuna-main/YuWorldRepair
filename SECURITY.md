# Security Policy

YuWorldRepair 会处理 Minecraft 世界文件，安全问题可能导致数据丢失、路径越界或备份被错误覆盖。

## 报告漏洞

请优先使用 GitHub 仓库的 **Private vulnerability reporting** 提交安全报告，不要在公开 Issue 中附带可直接利用的路径、世界文件、令牌或玩家数据。

报告应尽量包含：

- 受影响版本和产物文件名；
- Minecraft、NeoForge 和服务器实现版本；
- 最小复现步骤；
- 是否发生世界写入；
- 已脱敏的作业状态、异常和哈希；
- 你认为可能受影响的文件范围。

不要上传真实世界存档、玩家 UUID、服务器目录结构、授权令牌或未经脱敏的日志。

## 支持范围

当前接受安全修复的是最新维护 RC 和仓库主分支。实验离线工具始终要求世界副本和额外备份，不承诺直接操作生产世界。

## 安全原则

- 默认失败关闭；
- 不提供 `--force` 绕过哈希或覆盖缺口；
- 不在服务器仍持有世界锁时写入；
- 不把内置作业备份描述为整服备份；
- 不把未知 Mod 私有格式猜测为可安全删除数据。
